node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# source docs (not part of the app)
*.docx
~$*

# local env (secrets)
.env.local
.env

# local outreach list (contacts) — keep out of the public repo
docs/course-outreach.csv
